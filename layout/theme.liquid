<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ page_title }} - {{ shop.name }}</title>
  {{ content_for_header }}
  <style>
    body, html {
      height: 100%;
      margin: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      background-color: #ffffff; /* Optional: Add a background color */
    }
    .container {
      display: flex;
      justify-content: center;
      align-items: center;
      width: 30%;
      height: 28%;
    }
    .image-container img {
      max-width: 100%;
      height: auto;
      display: block;
    }
  </style>
</head>
<body>
  {{ content_for_layout }}
  <div class="container">
    <div class="image-container">
      {% if settings.landing_page_image %}
        <img src="{{ settings.landing_page_image | img_url: '1024x1024' }}" alt="Landing Page Image">
      {% else %}
        <p>Please upload an image in the theme settings.</p>
      {% endif %}
    </div>
  </div>
</body>
</html>
